import { NextRequest, NextResponse } from 'next/server';
import { getFirebaseWallet } from '@/lib/firebase-repo';
import { isNextResponse, requireUser } from '@/lib/session';
const COOLDOWN = parseInt(process.env.AD_REWARD_COOLDOWN_MINUTES ?? '2', 10); const CAP = parseInt(process.env.AD_REWARD_DAILY_CAP ?? '12', 10); const REWARD = parseInt(process.env.AD_REWARD_COINS ?? '5', 10); const today = () => new Date().toISOString().slice(0, 10);
export async function GET(request: NextRequest) { try { const auth = await requireUser(request); if (isNextResponse(auth)) return auth; const wallet = await getFirebaseWallet(auth.userId); if (!wallet) return NextResponse.json({ error: 'Wallet not found' }, { status: 404 }); const claimedToday = wallet.adRewardsDate === today() ? Number(wallet.adRewardsToday ?? 0) : 0; const last = wallet.lastAdRewardAt instanceof Date ? wallet.lastAdRewardAt : wallet.lastAdRewardAt ? new Date(String(wallet.lastAdRewardAt)) : null; const until = last ? last.getTime() + COOLDOWN * 60000 : 0; return NextResponse.json({ claimedToday, dailyCap: CAP, coinsPerAd: REWARD, cooldownUntil: until > Date.now() ? new Date(until).toISOString() : null }); } catch (error) { console.error('Ad reward status error:', error); return NextResponse.json({ error: 'Internal server error' }, { status: 500 }); } }
