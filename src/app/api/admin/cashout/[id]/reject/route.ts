import { NextRequest, NextResponse } from 'next/server';
import { getFirebaseCashout, rejectFirebaseCashout } from '@/lib/firebase-repo';
import { isNextResponse, requireAdmin } from '@/lib/session';
export async function POST(request: NextRequest, { params }: { params: Promise<{ id: string }> }) { try { const auth = await requireAdmin(request); if (isNextResponse(auth)) return auth; const { id } = await params; const cashout = await getFirebaseCashout(id); if (!cashout) return NextResponse.json({ error: 'Cashout request not found' }, { status: 404 }); if (cashout.status !== 'pending') return NextResponse.json({ error: `Cannot reject a cashout with status: ${cashout.status}` }, { status: 400 }); await rejectFirebaseCashout(id, auth.userId); return NextResponse.json({ ok: true, status: 'rejected' }); } catch (error) { console.error('Reject cashout error:', error); return NextResponse.json({ error: 'Internal server error' }, { status: 500 }); } }
